name: barry_wifi
description: Application BARRY WI-FI
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.1.0
  mobile_scanner: ^3.2.0

  # QR Code généré côté app
  qr_flutter: ^4.1.0

  # Stockage local (token, user_id, etc.)
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
