name: barry_wifi
description: Application BARRY WI-FI
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.1.0
  mobile_scanner: ^3.2.0
  qr_flutter: ^4.1.0

  shared_preferences: ^2.2.2

  # 🚀 Pour ouvrir WhatsApp + liens externes
  url_launcher: ^6.1.7

  # 🌍 Traductions
  flutter_localizations:
    sdk: flutter

  # 📸 Choisir une image de profil
  image_picker: ^1.0.7

  # 📁 Sauvegarder l'image dans la mémoire interne
  path_provider: ^2.1.2

  # 📊 Graphiques et statistiques
  fl_chart: ^0.66.0

  # 🌍 Géolocalisation
  geolocator: ^11.0.0
  geocoding: ^2.1.1

  # 🔐 Stockage sécurisé
  flutter_secure_storage: ^9.0.0

  # ⏱️ Timer et formatage de dates
  intl: ^0.18.1

  # 🎨 Animations modernes
  flutter_animate: ^4.3.0
  shimmer: ^3.0.0

  # 📱 Info appareil
  device_info_plus: ^9.1.1

  # 🔔 Notifications locales
  flutter_local_notifications: ^16.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/logo.png
