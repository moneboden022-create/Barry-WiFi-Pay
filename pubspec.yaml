name: barry_wifi
description: Application BARRY WI-FI - Premium 5G
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🎨 UI & Design
  cupertino_icons: ^1.0.6
  
  # 🌐 HTTP & API
  http: ^1.1.0
  
  # 📱 QR Code
  mobile_scanner: ^3.2.0
  qr_flutter: ^4.1.0

  # 💾 Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  path_provider: ^2.1.2

  # 🚀 URL & Links
  url_launcher: ^6.1.7

  # 🌍 Localisation
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

  # 📸 Image
  image_picker: ^1.0.7

  # 📊 Charts & Graphs
  fl_chart: ^0.66.0

  # 🌍 Géolocalisation
  geolocator: ^13.0.1
  geolocator_android: ^4.5.0
  geocoding: ^2.1.1

  # 🎨 Animations Premium
  flutter_animate: ^4.3.0
  shimmer: ^3.0.0

  # 📱 Device Info
  device_info_plus: ^9.1.1

  # 🔔 Notifications
  flutter_local_notifications: ^16.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

  # 📁 Assets
  assets:
    - assets/
    - assets/logo.png

  # 🔤 Fonts Premium (optionnel - utilise les fonts système si non disponibles)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Poppins-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Poppins-ExtraBold.ttf
  #         weight: 800
